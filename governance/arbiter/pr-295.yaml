# Harvested arbiter verdict — verbatim machine record of the public PR
# comment; append-only. See scripts/harvest-governance.mjs.
pr: 295
title: "Counts are measured, not estimated: the edition verb appends its own
  figures to every rationale"
url: https://github.com/ejhong/aletheia/pull/295
verdict: pass
reason: 5 of 5 seats affirm compliance; no seat finds a violation
outcome: merged
outcomeAt: 2026-09-16
judgedAgainst: 5f34dc6152
promptVersion: panel-v3
seats:
  - seat: Opus 5 (Anthropic)
    vote: complies
    rules: []
    reasoning: "The diff touches only tooling, protocol, and docs: it renames
      edition-v6 to protocols/archive/, adds edition-v7, adds a DECISIONS.md
      operator record, and changes src/pipeline/edition.ts so the verb appends
      measured word and account counts to every edition rationale. AGENTS.md is
      untouched, so §3.15's reserved-amendment rule is not engaged. The change
      strengthens rather than weakens a check: it replaces model-estimated
      figures (which the run records contradicted) with figures computed by the
      same countWords helper the run notes use, and edition-v7 explicitly
      forbids the model from stating counts of its own — squarely in line with
      §3.8 (never invent a locator/figure) and §3.13 (no false precision). The
      appended sentence is labelled \"Measured by the verb\", so the reader can
      tell which part of the rationale is the model's prose and which is
      machine-measured, satisfying §3.9/§3.14 rather than misattributing
      authorship. No citations or content/ records are added (verification
      tooling found no identifiers), and no confidential material is involved.
      The DECISIONS.md entry is stamped with date and channel as the
      constitution's founder-direction clause permits."
    cost:
      model: claude-opus-5
      inputTokens: 7280
      outputTokens: 640
      usd: 0.055593
  - seat: GPT-5.6 Sol (OpenAI)
    vote: complies
    rules:
      - §3.8
      - §3.14
      - §3.15
    reasoning: The change strengthens provenance by calculating article and account
      counts in the edition verb, labeling them explicitly as “Measured by the
      verb,” and reusing the same `countWords` helper in the run notes. The
      effective account count correctly uses the incumbent accounts when the
      candidate returns an empty inheritance list, so the recorded figure
      reflects the assembled edition rather than a model estimate. The protocol
      forbids the model from supplying these counts and requires disclosure when
      prose groups accounts differently, reducing misleading or internally
      inconsistent records. The decision log records the reason, date, and
      session channel, and the prior protocol is preserved in the archive. No
      citations or confidential material are added, AGENTS.md is untouched, and
      no constitutional check is weakened or bypassed.
    cost:
      model: gpt-5.6-sol
      inputTokens: 7552
      outputTokens: 700
      usd: 0.04472
  - seat: Gemini 3.8 Flash (Google)
    vote: complies
    rules:
      - §3.8
      - §3.14
      - §3.15
    reasoning: "The change replaces model-estimated counts in edition rationales
      with mechanically measured figures appended directly by the pipeline verb
      (`src/pipeline/edition.ts`), resolving inconsistencies identified in
      review note #294. AGENTS.md is untouched, no citations are fabricated, and
      the protocol update (`edition-v7.md`) enforces calibrated, verifiable
      records rather than ungrounded estimates. The decision and operating
      record are properly documented in `docs/DECISIONS.md` and
      `docs/AUTOMATION.md`."
    cost:
      model: gemini-3.8-flash
      inputTokens: 9405
      outputTokens: 432
      usd: 0.008674
  - seat: Grok 4.5 (xAI)
    vote: complies
    rules: []
    reasoning: 'The diff does not touch AGENTS.md, adds no citations or content
      ledger records, and publishes no confidence material. It strengthens
      provenance honesty: edition.ts appends verb-measured article and accounts
      figures to every rationale under an explicit "Measured by the verb" label,
      and edition-v7 forbids the model from stating its own counts—addressing
      contradicted estimates noted in #294 without weakening §3.15 checks.
      AUTOMATION.md and DECISIONS.md correctly document the protocol bump and
      founder/operator session; archiving edition-v6 is non-normative
      housekeeping. No fabrication, misattribution, or check-dodging is
      present.'
    cost:
      model: grok-4.5
      inputTokens: 9020
      outputTokens: 153
      usd: 0.019073
  - seat: GLM 5.3 Flash (Z.ai, via Venice)
    vote: complies
    rules:
      - §3.8
      - §3.14
      - §3.15
    reasoning: "The change replaces model-estimated word and account counts (which
      the run records showed were wrong, per review note #294) with figures
      measured mechanically by the verb itself and appended under an explicit
      'Measured by the verb' label — this is an improvement in §3.8/§3.14
      provenance honesty, not a fabrication, since the counts come from the same
      countWords helper the run record uses on the actual incumbent and
      candidate articles. The appended text is clearly attributed to the verb
      rather than passed off as the model's or a human's judgment, so no
      misattribution arises. The DECISIONS.md entry records a founder session
      direction with date and channel, which §3.15 expressly permits as an
      operating record, and AGENTS.md is untouched, so no constitution edit
      occurs. No citations are added, no confidence material is published, and
      no check is weakened — if anything the checks are tightened, since
      measured figures replace guesswork. The protocol archiving of edition-v6
      and the version bump to v7 with dated version notes are consistent with
      §3.14's versioning requirements."
    cost:
      model: z-ai-glm-5-3-flash
      inputTokens: 7464
      outputTokens: 590
      usd: 0.00176
cost:
  seats: 5
  complete: true
  inputTokens: 40721
  outputTokens: 2515
  usd: 0.1298
harvestedAt: 2026-09-16
